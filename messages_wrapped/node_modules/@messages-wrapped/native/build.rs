extern crate napi_build;

use std::io::Result;

fn main() -> Result<()> {
	println!("cargo::rerun-if-changed=proto/stats.proto");
	prost_build::compile_protos(&["stats.proto"], &["proto"])?;

	napi_build::setup();
	Ok(())
}
